# Test-only dependencies. Kept out of requirements.txt so none of this reaches
# the runtime image — the Dockerfile installs requirements.txt alone.
-r requirements.txt

pytest>=8.0
# fastapi.testclient is a thin wrapper over httpx; without it TestClient raises
# at import time rather than at first request.
httpx>=0.27
